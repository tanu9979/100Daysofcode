def print_primes_in_range(A, B):
    for i in range(A,B+1):
        sum=0
        for j in range(2,i):
            if i%j !=0:
                sum+=1
        if sum==(i-2):
            print(i,end=" ")
